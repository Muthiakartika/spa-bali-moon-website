/** Where WhatsApp messages from the Contact page say they came from. */
export const SOURCE = "Contact page";

/** Small gold capitals above a heading, e.g. "QUESTIONS OR BOOKINGS" (as the homepage FAQ label). */
export const capsLabel = "text-[0.8125rem] font-bold uppercase tracking-[0.2em] text-gold-deep";

/** Heading size inside the contact cards. */
export const cardTitle = "font-display text-[clamp(2rem,1.55rem+1.6vw,2.75rem)] leading-[1.08]";
